[project]
name = "backend"
version = "0.1.0"
description = "african-skin-detector-backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sqlmodel>=0.0.39",
]
